#!/bin/bash

# Exit immediately if a command exits with a non-zero status
set -e

echo "========================================="
echo " Starting Energy Information Application "
echo "========================================="

# Navigate to the script's directory to ensure relative paths work
cd "$(dirname "$0")"

